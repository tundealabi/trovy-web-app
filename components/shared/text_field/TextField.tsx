import { TextFieldProps } from '@mui/material';
import TextInputField from './TextField.styled';

const TextField = (props: TextFieldProps) => <TextInputField {...props} />;

export default TextField;
